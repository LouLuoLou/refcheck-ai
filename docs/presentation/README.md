# RefCheck AI — slide deck

Generate the PowerPoint file:

```bash
cd docs/presentation
npm install
npm run build
```

Output: **`RefCheck-AI-Deck.pptx`** (same folder).

Open in PowerPoint or Google Slides (File → Import). Replace dashed **image placeholders** with screenshots, your architecture diagram, and demo URL / QR.
